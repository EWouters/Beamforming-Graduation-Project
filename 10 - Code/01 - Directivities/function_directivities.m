

function [gain] = function_directivity(telefoon,theta, phi)
% Geen telefoon als input. We gebruiken 1 telefoon voor de directivities

% choose closest phi: (phi from 0 to 180 degrees)
% closest_phi(phi);
% choose nearest theta:  (theta from 0 - 360 degrees)
% closest_theta(theta);



gain = [1 1 1 1 1];


end 